HOW TO LAUNCH THE PROGRAM :
    // 1. Download the following libraries of python :(just write the following command in the terminal)
            - pip install tkinter
            - pip install customtkinter
            - pip install sqlite3
            - pip install bcrypt
            - pip install pygame
            - pip install Pillow
	    - pip install pygame_menu
    
    // 2. Launch the file named "login_page"

    // 3. Enjoy!!.